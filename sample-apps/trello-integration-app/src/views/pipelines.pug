extends includes/layout
block content
  section(class='container')
    div(class='row')
      h3 Select HubSpot pipeline
    div(class='row')
        table
          tbody
            each pipeline in pipelines
              tr
                td
                  a(class='button' href=`/mappings/board/${boardId}/pipeline/${pipeline.id}`) #{pipeline.label}
