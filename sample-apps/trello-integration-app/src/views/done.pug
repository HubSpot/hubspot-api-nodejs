extends includes/layout
block content
  .container
    div(class="row")
      div(class="column")
        h4 Extension card with id=#{cardId} is ready to use. Now you can test trello integration via HubSpot interface deal page.
